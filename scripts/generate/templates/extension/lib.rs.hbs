{{#if agg}}
mod agg;
{{/if}}
{{#if scalar}}
mod scalar;
{{/if}}
{{#if vtab}}
mod vtab;
{{/if}}

use limbo_ext::register_extension;

register_extension! {
    {{#if vtab}}
    vtabs: { vtab::{{struct_prefix}}VTab }
    {{/if}}
    {{#if agg}}
    aggregates: { agg::{{struct_prefix}}Aggregate }
    {{/if}}
    {{#if scalar}}
    scalars: {scalar::{{name}}_scalar}
    {{/if}}
}

#[cfg(test)]
mod tests {}

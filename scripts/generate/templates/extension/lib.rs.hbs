{{#if agg}}
mod agg;
{{/if}}
{{#if scalar}}
mod scalar;
{{/if}}
{{#if vtab}}
mod vtab;
{{/if}}

{{#if agg}}
use agg::{{struct_prefix}}Aggregate;
{{/if}}
{{#if scalar}}
use scalar::register_{{name}}_scalar;
{{/if}}
{{#if vtab}}
use vtab::{{struct_prefix}}VTab;
{{/if}}

use limbo_ext::register_extension;

register_extension! {
    {{#if vtab}}
    vtabs: { {{struct_prefix}}VTab }
    {{/if}}
    {{#if agg}}
    aggregates: { {{struct_prefix}}Aggregate }
    {{/if}}
    {{#if scalar}}
    scalars: { {{name}}_scalar }
    {{/if}}
}

#[cfg(test)]
mod tests {}
